DATABASE_URL=postgres://trackward:trackward@localhost:5432/trackward?sslmode=disable
LISTEN_ADDR=0.0.0.0:3000
S3_BUCKET=trackward-artifacts
S3_ENDPOINT=http://localhost:9000
S3_REGION=us-east-1
AWS_ACCESS_KEY_ID=minioadmin
AWS_SECRET_ACCESS_KEY=minioadmin

# Gateway
GATEWAY_LISTEN_ADDR=0.0.0.0:4000
LEDGER_URL=http://localhost:3000
# Comma-separated mapping of tool -> backend URL, e.g. bash=http://bash.svc:8080/exec
TOOL_ROUTES=
# Comma-separated list of tools that require human approval
GATED_TOOLS=
# Retrieval backend URL (optional)
# RETRIEVAL_BACKEND=http://localhost:7000/search

# OpenTelemetry — set to enable OTLP/gRPC span export to the collector.
# OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4317
